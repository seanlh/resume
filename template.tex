%!TEX TS-program = xelatex
%!TEX encoding = UTF-8 Unicode

\documentclass[$fontsize$, a4paper]{article}

% LAYOUT
%--------------------------------
% Margins
\usepackage{geometry}
\geometry{$geometry$}

% Do not indent paragraphs
\setlength\parindent{0pt}

% Enable multicolumns
\usepackage{multicol}
% \setlength{\columnsep}{-6cm}

% Uncomment to suppress page numbers
\pagenumbering{gobble}

% LANGUAGE
%--------------------------------
% Set the main language
$if(lang)$
\usepackage{polyglossia}
\setmainlanguage{$lang$}
$endif$

% TYPOGRAPHY
%--------------------------------
\usepackage{fontspec}
% converts TeX-style quotes/dashes to Unicode equivalents
\defaultfontfeatures{Ligatures=TeX}
\setromanfont [Ligatures={Common}, Numbers={OldStyle}]{$mainfont$}
\DeclareFontShape{TU}{HoeflerText(0)}{m}{sc}%
       {<-> "Hoefler\space Text:Letter\space Case=Small\space Caps"}{}
% Cool ampersand
\newcommand{\amper}{{\fontspec[Scale=.95]{$mainfont$}\selectfont\itshape\&}}

% MARGIN NOTES
%--------------------------------
\usepackage{marginnote}
\newcommand{\note}[1]{\marginnote{\scriptsize #1}}
\renewcommand*{\raggedleftmarginnote}{}
\setlength{\marginparsep}{7pt}
\reversemarginpar

% HEADINGS
%--------------------------------
\usepackage{titlesec}
\titleformat{\section}{\rmfamily\mdseries\Large}{}{0pt}{}
\titleformat{\subsection}{\rmfamily\mdseries\scshape\normalsize}{}{0pt}{}
\titleformat{\subsubsection}{\rmfamily\bfseries\normalsize}{}{0pt}{}

% LIST
%--------------------------------
\usepackage{enumitem}

% PDF SETUP
%--------------------------------
\usepackage{hyperref}
\hypersetup
{
  pdfauthor={$name$},
  pdfsubject={$name$'s Résumé},
  pdftitle={$name$'s Résumé},
  colorlinks, breaklinks, xetex, bookmarks,
  filecolor=black,
  urlcolor=[rgb]{0.117,0.682,0.858},
  linkcolor=[rgb]{0.117,0.682,0.858},
  linkcolor=[rgb]{0.117,0.682,0.858},
  citecolor=[rgb]{0.117,0.682,0.858}
}

% DOCUMENT
%--------------------------------
\begin{document}

{\LARGE $name$}

\begin{multicols}{2}
  \columnbreak
  $for(address)$
    $address$\\
  $endfor$
  \href{tel:$phone$}{$phone$}\\

  \columnbreak
  \href{mailto:$email$}{$email$}\\
  $for(urls)$
    \href{https://$urls$}{$urls$}\\
  $endfor$
\end{multicols}

$if(intro)$
  $intro$
$endif$

$if(skills)$
  \subsection*{Skills}
  \begin{itemize}[noitemsep, topsep=2pt]
      $for(skills)$
        \item $skills$
      $endfor$
  \end{itemize}
$endif$

\section*{Work Experience}
$for(experience)$
  \textsc{$experience.employer$}\\
  $if(experience.location)$
    $experience.location$\\
  $endif$
  $for(experience.roles)$
    \note{$experience.roles.years$}\emph{$experience.roles.title$}
    $if(experience.roles.points)$
      \begin{itemize}[noitemsep, topsep=0pt, partopsep=0pt]
        $for(experience.roles.points)$
          \item $experience.roles.points$
        $endfor$
      \end{itemize}
    $else$
      \par
    $endif$
  $endfor$
  $sep$\vspace{16pt}
$endfor$

$if(education)$
  \section*{Education}
  $for(education)$
    \textsc{$education.institute$}$if(education.location)$, $education.location$\\$endif$
    \note{$education.year$}\textbf{$education.subject$}$if(education.degree)$, $education.degree$$endif$\\
  $endfor$
$endif$

$if(languages)$
  \section*{Languages}
  $for(languages)$
    \emph{$languages.language$} ($languages.proficiency$)\\
  $endfor$
$endif$

\end{document}
